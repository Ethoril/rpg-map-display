// @ts-check

/**
 * Calcul du masque d'arêtes de grille bloquées à partir de la géométrie de l'étage.
 * Stub pour le lot 1a : retourne un Set vide.
 * L'implémentation géométrique réelle sera réalisée au lot 2.
 *
 * @param {import('../core/types.js').Level} level
 * @param {import('../grid/GridAdapter.js').GridAdapter} grid
 * @returns {Set<string>} Set de clés d'arêtes ("cellKey1|cellKey2")
 */
export function computeBlockedEdges(level, grid) {
  return new Set();
}
